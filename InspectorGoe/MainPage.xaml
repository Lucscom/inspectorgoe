<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InspectorGoe.MainPage"
             xmlns:contentviews="clr-namespace:InspectorGoe.ContentViews"
             xmlns:viewModel="clr-namespace:InspectorGoe.ViewModels"
             xmlns:container="clr-namespace:InspectorGoe.Container">


    <AbsoluteLayout AbsoluteLayout.LayoutBounds="0,0,1,1"
                    AbsoluteLayout.LayoutFlags="All"
                    x:Name="abolutLayout">

        <container:PinchPanContainer AbsoluteLayout.LayoutBounds="0,0,1,1"
                                     AbsoluteLayout.LayoutFlags="All">

            <AbsoluteLayout x:Name="absoluteMap"
                            WidthRequest="2600"
                            HeightRequest="1463"
                            TranslationX="-600">

                <Image Source="map.png"
                       AbsoluteLayout.LayoutFlags="All"
                       AbsoluteLayout.LayoutBounds="0,0,1,1"
                       WidthRequest="2600"
                       HeightRequest="1463"/>


                <Button AbsoluteLayout.LayoutBounds="2000,500,30,30"/>

            </AbsoluteLayout>
        </container:PinchPanContainer>

        <Grid AbsoluteLayout.LayoutBounds="0,0,1,0.99"
              AbsoluteLayout.LayoutFlags="All">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="230" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


        <!--Inhalt-->
            <ScrollView>
                <VerticalStackLayout Padding="5" 
                                     BindableLayout.ItemsSource="{Binding MrXticketsCollection}" 
                                     HorizontalOptions="Start"
                                     VerticalOptions="End"
                                     >
                    
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <contentviews:MrX_Ticket/>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
            </ScrollView>

            <ScrollView Grid.Row="1"
                    Grid.Column="1"
                    Orientation="Horizontal">

                <HorizontalStackLayout BindableLayout.ItemsSource="{Binding DetectivesCollection}"
                                       HorizontalOptions="Center">

                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <contentviews:PlayerCard/>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </HorizontalStackLayout>
            </ScrollView>

            <StackLayout Grid.Row="1" Padding="5">
                <contentviews:MrXCard BindingContext="{Binding MisterX}"/>

            </StackLayout>

            
        </Grid>
    </AbsoluteLayout>



</ContentPage>
